# ADR: D-pure 纯工具桥（决策记录与开放问题）

> 状态：**主体已完成并合并入 `refactor/pure-tool-bridge`**（原实施 TODO 见 git 历史：阶段 0-5 分别落地，随后一轮全仓瘦身又删除了无调用方的 purge/inject RPC 链、划词捕获链等）。本文件只保留仍有价值的决策记录与尚未关闭的开放问题。

## 决策记录

| 日期 | 结论 |
|---|---|
| 2026-09-02 | 侧栏聊天全删；只保留状态展示（最多 header「操作 xxx 页面中」）。 |
| 2026-09-02 | 保留能力 = 操作真实浏览器（登录态保留、真实 Chrome），服务非本地 E2E。 |
| 2026-09-02 | 单 Chrome 双会话（会话A→webA、会话B→webB）机制已支持（按 sessionId 路由、后台标签照跑）；绑定/后台加固列为后备（原阶段 3）。 |
| 2026-09-02 | **O1**：不依赖上游迁移分支，在 fork 里自研 0.1.2 传输迁移（`apiProxy` → `typertGateway`/`connection`）；上游 `feat/dsh-0.1.2-migration` 仅作只读参考，**不合并**。 |
| 2026-09-02 | **O2**：会话分组去向**搁置**——扩展不再经桥建会话，桥通道级分组无意义；将来如需分组上移 host 服务级。 |
| 2026-09-02 | **O4**：未绑定会话首次 `browser_navigate` → 自动开新标签并绑定；设置/信任配置放 options 页；审批走 popup/状态窗。 |
| 2026-09-02 | 版本线：运行时与依赖统一 pin 到 `0.1.2-rc.1`（0.1.2 稳定发布后再切正式 tag）。 |
| 2026-09-09 | 版本线升级：运行时与依赖统一对齐 dsh `0.1.5-alpha.1`（当时运行 harness 已是 0.1.5-alpha.1；peer/devDeps/root CLI 与 lockfile 同升，见 [升级记录](dsh-0.1.5-alpha-upgrade.md)）。 |
| 2026-09-10 | 版本线升级：运行时与依赖统一对齐 dsh `0.1.5-rc.1`（运行 harness 已切到 `latest` = 0.1.5-rc.1；peerDeps/devDeps/root CLI 与 lockfile 同升，见 [升级记录](dsh-0.1.5-rc-upgrade.md)）。 |
| 2026-09-15 | **O4 修订（同意顺序）**：未绑定会话首次 `browser_navigate` 改为「先按目的地取信任/审批，通过后才建标签并加载」。此前 `chrome.tabs.create({url})` 在审批之前执行，用户未授权也会发生真实跳转，随后还常以 "The page changed while approval was pending" 报失败。副作用：首次导航不再自动附带落地快照，模型需自行再调一次 `browser_snapshot`。 |
| 2026-09-15 | **移除浏览器 e2e 测试**（`packages/browser/bridge-browser/tests/e2e/`）：本项目不做自动化浏览器回归，浏览器内行为改为在真实 Chrome 里手工验证；`playwright-core` 仍留在 bridge-browser 的 devDependencies，因为 `benchmark/` 的 `install-browser` 脚本引用它的 CLI。 |
| 2026-09-15 | **握手兼容字段 `textOnly` 双向保留**：`hello`（扩展→宿主）与 `hello.ok`（宿主→扩展）都继续携带 `textOnly: true`。两个方向的旧代码都用 `caps.textOnly === true` 校验对方帧，缺了就丢帧 → 对端永远等不到 ack，UI 停在 Connecting…。实测：宿主热重载/重启成新代码后，Chrome 里加载的旧扩展立刻连不上（新宿主不再发该字段）。因此 `BridgeCaps.textOnly` 是**双向兼容标记**，不是能力声明（能力看 `vision`）。宿主另按扩展上报的 `vision` 决定是否索取截图。待所有部署实例都升级后可删除。**（2026-09-16 已按下面的版本协商删除）** |
| 2026-09-15 | **扩展镜像目录会过期**：`scripts/install.sh` 用 `rsync -a --delete-after` 把 `extensions/dsh-browser/dist/` 同步到 `~/.dsh/browser-extension/`，Chrome 加载的是后者。改了源码只跑 `pnpm build` 不会更新该镜像，必须重跑安装脚本或手工同步，否则 Chrome 里一直是旧构建（这次 Connecting… 的一半原因）。 |
| 2026-09-16 | **`google_drive_export` 收窄到 Docs / Sheets**：只认 `/document/d/…` 与 `/spreadsheets/d/…`（含 `spreadsheets.google.com`）；Slides、Drive 文件及其它链接不再走导出，改用浏览器读（`browser_navigate` → `browser_snapshot` / `browser_capture` / `browser_dom_query`）。相应地：导航与页面工具的 Drive 拦截只对这两种 URL 生效（Slides/Drive 页面现在是普通页面），宿主工具描述与系统提示同步收窄；扩展侧 URL 解析抽成 `src/background/gdrive-url.ts` 并配测试。 |
| 2026-09-16 | **缓冲区改随绑定预热（eager attach）**：实测 `session-2072d696`（CP 邮件权限调查）里 `browser_network` 首次调用返回 `none new`——缓冲区从第一次调用才开始录，页面加载期的请求全丢了。改为：绑定受控标签页即 attach 并 enable Runtime/Log/Network（上限 3 个绑定标签页、串行化、幂等），解绑/关闭/关掉调试能力即 detach；用户点掉调试提示条后靠 `isSessionAttached` 识别并重新预热。 |
| 2026-09-16 | **省 token 第一、二步落地**（调研见 `docs/dom-caching-feasibility.md`）：① 新增 `browser_dom_query`（只读窄查询，回字段 + 可访问名 + 唯一选择器，替代 eval 翻 DOM），归入页面读取审批并受共享策略约束；② 未变化的 nav/header/footer 条目折叠成一行（编号仍有效）；③ 导航后自动附的快照上限从 32k 降到 8k。实测基线（`benchmark/tokens.mjs`）：7 个会话 310,546 字符 ≈ 77.6k token，其中 navigate 30.7%、snapshot 24.6%、eval 21.0%；页面文本逐字节重复率 0%，故不做去重。 |
| 2026-09-16 | **额度不够可立即行动**：快照在被条目上限截断时，除原有计数外，额外列出被丢掉的前 8 个控件及其**经校验唯一的 CSS 选择器**（`Omitted by the inventory cap (click them by selector)` 区块），并把提示语从「去看更多文本」改成「清单上限 N 条，被丢弃的控件可用选择器直接点」。不改成自动附图：默认快照本来就带同刻截图，而对「点哪个」这个问题图给不了句柄，选择器才是出路。 |
| 2026-09-16 | **点击看得见的元素，而不是盲点**：① `browser_click`/`browser_type` 增加 `selector`（与 `index` 并列；在目标 frame 解析、优先可见匹配、先 `scrollIntoView`，仍走审批与稳定检测）；② 可访问名增加 `title`/`data-original-title`/`data-tooltip`/`data-title`/`aria-description`，最后回退到图标类名（`icon-specialist-email` → specialist email）；③ 清单排序改为「视口内 + 非框架区」优先，防止导航链接吃光 `maxInteractiveItems` 额度。起因：`session-2dc02c74` 中目标图标控件既无可访问名、又被额度挤出清单，模型只能退回 `browser_eval` 盲点并触发页面 alert。 |
| 2026-09-16 | 新增 `browser_dialog`：会话 `session-2dc02c74` 里模型在 `browser_eval` 中执行 `button.click()` 触发页面校验 `alert`，原生弹窗冻结渲染进程，该调用 51 秒后被取消、随后所有内容脚本工具一起挂住。处理办法走 CDP `Page.handleJavaScriptDialog`（accept/dismiss + prompt 文本），并把 `Page.javascriptDialogOpening` 记进会话状态与控制台缓冲。归入调试工具组（需开启调试能力）与不可信任动作（确认弹窗可能提交不可逆操作）。 |
| 2026-09-16 | **调试能力改为显式同意（默认关）**：扩展新增设置 `allowExtensionDebug`，只在该开关打开且存在 `chrome.debugger` 时，`hello` 才上报 `debugger: true`；宿主据此**动态注册/注销**四个调试工具（`browser_capture` / `browser_console` / `browser_network` / `browser_eval`），关闭时模型在工具表里看不到它们；扩展侧另有一道 `debugToolRefusal` 兜底，防止旧宿主仍持有工具时被调用。协议字段 `vision` 被 `debugger` 取代（`textOnly` 仍作旧宿主握手标记）。机制：`BridgeServerDeps.onCapabilities` 在握手/替换/断连时通知工具注册表。 |
| 2026-09-16 | 诊断出 `browser_eval` 在真实会话里失败的原因：不是 CDP 失败，而是**审批生命周期**——eval 属「不可信任动作」，每次调用都要弹窗；实测两次失败分别耗时 12.9s 与 0.1s、伴随一次 2.7s 成功，说明是提示被撤回/调用被取消（切页、绑定、宿主撤回）而非超时。处置：① 新增设置项 `trustJsExecution`（默认关），开启后 origin 信任也覆盖 JS 执行；② 把「已取消」的返回码从 `bridge-closed` 改为 `action-failed`，文案改为「什么都没执行 + 可能原因 + 下一步」。 |
| 2026-09-15 | 调试类工具落地五件：`browser_console`、`browser_network`（列表 + 响应体 + 内存内响应替换 `Fetch.fulfillRequest`）、`browser_eval`、`browser_block`、`browser_headers`。CDP 三件复用截图的 attach 通道（事件流按 tab 常驻 + 环形缓冲 + cursor 增量读）；阻断与改头走 DNR 的 session 规则并带 `tabIds`，只作用于受控页。`browser_eval`/改头/响应替换永不信任跳过，阻断可按 origin 信任。断点/单步未做。 |
| 2026-09-15 | 视觉能力落地：`BridgeCaps` 去掉 `textOnly` 改为可选 `vision`；`browser_snapshot` 默认附带同刻截图（`visual: false` 可关）；新增 `browser_capture`（Chrome 侧 `chrome.debugger` + `Page.captureScreenshot`，仅内存、不落盘，经 attachment 作为 image block 交给模型）。详见 [DevTools 调研](devtools-tools-feasibility.md) §0。 |
| 2026-09-16 | **握手改为版本协商，删除 `textOnly`**：`BridgeCaps` 增加 `proto`（握手版本，当前 2；缺失即旧构建，读作 1）与 `toolset`（扩展能力级别，当前 1；缺失读作 0），`textOnly` 双向删除。理由：今天三次故障全是两半构建错配（旧扩展→Connecting…、新宿主+旧扩展→`selector` 参数报错、新描述+旧守卫→自相矛盾），而旧机制下形状不匹配的帧被静默丢弃，只能靠人肉探测。处置：① 宿主按扩展声明的 `toolset` **裁剪工具面**——级别 0 时不注册 `browser_dom_query`/`browser_block`/`browser_headers`，`browser_click`/`browser_type` 的 schema 去掉 `selector` 并在描述里写明改用 `index`，同时把仍传进来的 `selector` 就地翻成一句可操作拒绝（模型可能拿着切换前的 schema）；② 宿主无法服务更新协议时用 WebSocket 关闭码 `4003` + 可读原因拒绝（"restart dsh: extension bridge protocol N is newer…"），扩展把宿主原因原样显示；③ 扩展侧新增握手提示（`host-rejected` / `host-silent` / `host-older` / `host-newer`），在选项页与侧栏各显示一行并点名下一步（重启 dsh / 重载扩展）——旧的"猜不出为什么连不上"因此变成一条明确指引。 |
| 2026-09-16 | **SVG 控件不再是二等公民**（起因 `session-03780cd2`：模型要"点第 10 页"，四个环节全堵）：① `isVisible` 的 `instanceof HTMLElement` 把**所有 SVG 元素**判成不可见（Slides 胶片栏缩略图、图表/地图控件），于是 `browser_dom_query` 的 `visible` 字段撒谎、选择器命中 SVG 也报"不可见"而拒点；改为接受任意 `Element` + computed style + rect。② `uniqueSelector` 用 `el.className`（SVG 上是 `SVGAnimatedString`）与 `tagName.toLowerCase()`（破坏 SVG 大小写），SVG 控件因此拿不到句柄；改为 `getAttribute('class')` + `localName`。③ 点击用 `(el as HTMLElement).click()`，而 `SVGElement` 没有 `click`，必然 TypeError；改为 HTML 走 `click()`、其余派发 bubbling 的 `MouseEvent('click')`。④ 清单仍只收 HTML 交互元素（SVG 不在 `INTERACTIVE_SELECTOR` 里），正确路径是先用 `browser_dom_query` 拿 `selector=` 再点。 |
| 2026-09-16 | **`browser_dom_query` 的可访问名必须带标签**：每行原先是 `[1] g "10Copyright…" class=… text=…`，那个引号串是**计算出的可访问名**，却与字段值同位置且无标签，模型据此构造了 `[aria-label^="10Copyright"]` 属性选择器（该属性并不存在）→ 0 匹配，白费两次调用。改为 `name="…"`（同时接受 `name` 作为字段名、总在行首输出），宿主工具描述写明「`name=` 是计算值，不要据此构造属性选择器，请用给出的 `selector=`」。 |
| 2026-09-16 | **授权后的复核要说得清，且不因辅助 frame 抖动而作废**：`session-03780cd2` 里 `browser_eval` 在信任短路下**自动通过**（`trustJsExecution` 开 + `docs.google.com` 受信），随后却返回「The page changed while approval was pending」，而页面根本没变。三处修正：① `sameTargetDocument` 不再拿 `documentId ?? url` 混种键比较（frame 树短暂不可用时 url-only 的兜底列举会被读成"导航过"），改用 `sameFrameDocument`——两边都有 `documentId` 才比 id，否则比 url；快照基线与元素编号的新鲜度判断同步改用该函数。② 失败文案点名实际差异（新增/移除的 origin、frame 消失、frame 导航前后 URL）。③ `browser_eval` 的同意边界收窄到**表达式真正执行的那个 frame**（`browser_dialog`/`browser_headers` 仍按整页，它们的作用域本就是整页）。 |
| 2026-09-16 | **页面文本可搜索：`browser_get_text` 增加 `find`/`context`（工具集级别 2）**。起因 `session-33132bb6`：模型为了拿到"第 10 页"的文本，在 `/htmlpresent` 上用 5 轮 `browser_eval` 逐层切 DOM（列 id → 读 innerText → 按 `N of 142` 切分 → 数页码 → 找容器），只因为「按文本定位」这件事在工具面上不存在：`browser_get_text` 会在 32k 截断、`browser_dom_query` 只能按 CSS 选择器找，而那页几乎没有可用 id。处置：① 扩展侧 `getTextAction` 支持 `find`（字面、大小写不敏感）与 `context`（每侧字符数，默认 300、上限 2000），**在完整文本上搜索**（不是截断后的头部），返回每处命中的有界窗口并标注 `«命中»` 与字符区间；无命中时报告"搜索了多少字符"，窗口宽度会按 `maxChars` 自动收窄以保证命中不被截掉。② `toolset` 从 1 升到 2，并显式区分 `TOOLSET_SELECTOR_TARGETS=1`：级别 1 的扩展（就是当前 Chrome 里那份）保留选择器与 DOM/规则工具但没有 `find`，宿主按级别切换 schema，且把仍传进来的 `find` 就地翻成"读取全文并自己搜索 / 重载扩展"。③ 系统提示加一句：要"某页/某段讲了什么"先一次 `browser_get_text`（可带 selector 或 find+context）再自己切分，不要用多轮 eval 翻 DOM；要外观/画布内容才用 capture，且该标签页不能开 DevTools。 |
| 2026-09-16 | **`browser_image`：页面里的图片按引用取回（工具集级别 3）**。起因：报表页内容基本全是内嵌图（`session-86019c24`，正文只有 3878 字符），`browser_capture` 修复后仍拿不到"整张图"——视口截图 1519×865 要滚三次，而页面里那张图本身是 1338×1722，模型于是走 `curl` 下载页面 → Python 抠 base64 → 落盘 → `read_image`，绕出了浏览器工具链。处置：新增页面读取类工具 `browser_image { selector | index, frame }`——内容脚本只回报**图片位置**（`<img>`/`<canvas>`/SVG `<image>`/CSS background；canvas 由页面自身导出，被跨源污染时明确拒绝并指向 `browser_capture`），后台用 host 权限 `fetch`（不受 CORS 限制，`credentials: include` 以覆盖登录后图片；`data:` 直接解码）→ **能装下就原样透传**，超准入才栅格化重编码。不做无谓缩放的理由是实测 + 配置：本机准入是 20 MiB/64 MP/边长 8192（宿主 `captureLimits()` 转发的正是这三个），而归一化（4.19 MP/4 MiB）由 DSH 自己做，两条路都要过同一道归一化，所以在扩展里先缩一次只会二次损失细节。实测两份报表页 24 张内嵌 PNG 全部 pass-through（0.38–2.30 MP、16–109 KiB、最长边 1722px）。不需要 `chrome.debugger`，因此 DevTools 开着、后台标签页都能用；图片仍只走内存 + attachment，扩展不落盘。"仅当归一化真的缩过时把原图另存供裁剪"这条兜底本次未做，先记在 TODO。 |
| 2026-09-16 | **CDP 会话收敛为单一属主（`debugger-session.ts`）**：截图与 console/network 缓冲各自 attach，Chrome 对"同一扩展 + 同一 target"的第二次 attach 会返回 `Another debugger is already attached to the tab with id: N.`（源码里该串只由 `debugger_api.cc` 的 `FindClientHost()` 产生，即**本扩展自己**已持有；DevTools 占用走的是 `Cannot attach to this target.`），而我们把前者翻译成了"Close DevTools"，于是"绑定即预热"上线后每次截图都被自家会话顶掉、还冤枉 DevTools（实测 `session-33132bb6` 15:10、`session-ccab8e30` 15:10 两次）。处置：新建 `src/background/debugger-session.ts` 作为 attach 的唯一入口——按标签页引用计数，截图 `acquire` 复用缓冲的常驻 `hold`、只释放自己那份、绝不断开别人的会话；`already attached` 一律视为自家会话并复用；`Cannot attach to this target.` 再用 `chrome.debugger.getTargets()` 的 `attached` 区分"DevTools 占用"与"受保护页面"两种文案；并把 `attachAndEnable` 失败后仍记 `attached = true` 的谎报去掉（否则复用失败的会话会永久卡住缓冲）。`capture.ts` 与 `devtools.ts` 都改为使用该模块。 |
| 2026-09-16 | **新建标签页要等首个文档提交，且"不能注入"要分两种说法**：`session-33132bb6` 里 `browser_navigate` 之后 0.6s 的 `browser_wait` 在 31ms 内失败，报「The current page does not support browser operations. Switch to a standard http or https page.」——而用户就在正确的页面上。根因：`chrome.tabs.create({url})` 在文档提交前就返回，绑定的是 `url` 还是空的快照，内容脚本自然不在，兜底分支 `isInjectablePage(tab.url)` 为假。处置：① 新增 `src/background/tab-commit.ts` 的 `waitForTabCommit`（轮询 `tabs.get` 直到出现 http(s) URL，上限 10s，标签页被关或超时则回落创建时的快照，不让导航整体失败），`openBoundTab` 绑定提交后的快照；导航成功文案同步改成「新标签已开始加载」。② `tools.ts` 把兜底文案拆开：`url` 为空/`about:blank` → 「受控标签页还没加载完第一个文档，稍后再调 browser_wait / browser_snapshot」；`chrome://`、`file://` 等真正不可注入的页面仍用原文案。旧文案会把"新标签正在加载"误导成"换个页面"，模型因此换工具而不是重试。 |
| 2026-09-16 | **提示词：拒绝是边界，不是谜题**，并修掉一处自相矛盾。① 新增两句：不得为取同一内容换传输通道（curl / 阅读代理 / 无鉴权导出 URL / 换个工具）——那是绕过用户的闸门而非解决问题；两条工具互相指向对方拒绝的路线时，停下来把矛盾告诉用户，不要用试调用去探策略边界（起因 `session-4fa0d38d`，模型自述 "curl in bash is not restricted by the DSH guard"）。② 提示词里「Google Docs/Sheets/Slides/Drive 链接都不可当网页读，一律用 `google_drive_export`」是导出收窄前的残留，与工具描述、扩展守卫直接矛盾（`session-4fa0d38d` 里模型正是引用了它）；改为「只有 Docs/Sheets 走导出，Slides 与 Drive 文件是普通页面，用浏览器工具读」。 |

## 开放问题

- **O3 协调事件集合**：tab-affinity 交接时机曾依赖 `turn/start|end`，审批瞬时态依赖 `question/*`；砍聊天后 `question/*`（审批作答方变为宿主标准客户端）的去留需逐条核对扩展后台消费。
- **O5 上游同步策略**：本 fork 独走本重构；完成后是否反向 PR 上游，以及 upstream main 若合入 C（0.1.2 迁移）后本分支如何处理，待后续讨论。

## 参考

- 实施范围与阶段划分：git 历史 `refactor/pure-tool-bridge` 提交说明。
- 运行环境：dsh `0.1.5-rc.1`（peer/devDeps/root CLI 统一此线，2026-09-10 升级）。
